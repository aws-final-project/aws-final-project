spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL95Dialect

  datasource:
    url: "jdbc:postgresql://${DB_HOST}/${DB_NAME}"
    username: ${DB_USER}
    password: ${DB_PWD}